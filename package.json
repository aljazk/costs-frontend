{
  "type": "module",
  "scripts": {
    "start": "http-server ./src -p 80",
    "dev": "nodemon --exec http-server ./src -c-1",
    "test": "npx mocha --recursive --parallel",
    "test-watch": "npx mocha --watch --recursive --parallel"
  },
  "devDependencies": {
    "chai": "^4.3.10",
    "http-server": "^14.1.1",
    "jest": "^29.7.0",
    "mocha": "^10.2.0",
    "nodemon": "^3.0.1"
  }
}
